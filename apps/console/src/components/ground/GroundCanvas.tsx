'use client';

// SOURCING: hand-roll (MaterialLayer). Spec 34 + amendment 35. This file is
// the ledger alias: GroundCanvas names the ambient frame layer; MaterialLayer
// is the WebGL implementation that replaced the Canvas2D dot field.

export { MaterialLayer as GroundCanvas, MaterialLayer } from './MaterialLayer';
