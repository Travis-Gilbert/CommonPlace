export function writeHydrationCache(value: string): void {
  // persistence-cache: reason=accelerates hydration from durable server layout
  window.localStorage.setItem('hydration-cache', value);
  window.localStorage.setItem('unannotated-adjacent-cache', value);
}
