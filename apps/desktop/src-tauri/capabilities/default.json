{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the CommonPlace shell and its PET extension window",
  "windows": ["main", "pet"],
  "permissions": [
    "core:default",
    "opener:default",
    "deep-link:default",
    "theorem-pet:default"
  ]
}
