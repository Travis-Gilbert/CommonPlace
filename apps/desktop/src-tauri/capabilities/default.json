{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local capability for the chrome-free PET extension window",
  "windows": ["pet"],
  "permissions": [
    "core:default",
    "theorem-pet:default"
  ]
}
