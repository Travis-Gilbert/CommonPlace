'use client';

/**
 * Re-export shim (HANDOFF-GREENFIELD-CONSOLE G0): the database host hooks now
 * live in packages/block-view. Existing import paths keep working.
 */
export * from '@commonplace/block-view/host/use-database';
