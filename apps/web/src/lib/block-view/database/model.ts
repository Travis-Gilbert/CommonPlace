/**
 * Re-export shim (HANDOFF-GREENFIELD-CONSOLE G0): the resolved database model
 * now lives in packages/block-view. Existing import paths keep working.
 */
export * from '@commonplace/block-view/database/model';
