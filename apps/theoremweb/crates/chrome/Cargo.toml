[package]
name = "theoremweb-chrome"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Licensed theme tokens and CSS variable bridge for TheoremWeb"

[lib]
path = "src/lib.rs"

[workspace]

[lints.rust]
unsafe_code = "forbid"
