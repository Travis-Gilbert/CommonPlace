interface:
  display_name: "CommonPlace"
  short_description: "Repo-specific patterns and workflows for CommonPlace"
  default_prompt: "Use the CommonPlace repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true