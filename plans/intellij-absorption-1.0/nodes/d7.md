# d7 — single text algebra (decision)

- kind: decision
- controller: agent
- gist: D7 row: `rustyred-thg-text-model` re-backs on `lapce-xi-rope`; `RopeDelta` is the wire, computation, and rebase algebra end-to-end; the substrate persists deltas in a storage encoding it owns (byte-offset insert/delete runs, base length, rev) mapping one-to-one onto `RopeDelta` at the store boundary; references STUDY-RHIZOME-REBASE for leader-follower policy.
- provenance: HANDOFF-LAPCE-FORK-SPIKE-1.0 board amendment 4 (D7 row). Work sibling: `d7m` (migration).

## Blueprint

Record the D7 decision of record (already stated in the handoff; this node pins it to the board) and the matching amendment row on the fleet manifest.

## Obligations

- O-d7.1: D7 decision paragraph recorded here (algebra chosen, storage encoding owned by substrate, store-boundary conversion, STUDY-RHIZOME-REBASE reference for leader-follower policy). Discharge: paragraph in this file.
- O-d7.2: Fleet manifest amendment row on `Theorem/docs/plans/jetbrains-fleet-port/CLOSURE-MANIFEST.md` — the `fleet/andel` candidate section's rope row ("Use a mature Rust rope behind the new model because the oracle presents no requirement that justifies owning another rope implementation"): record that ropey is superseded by `lapce-xi-rope` per D7, with reason, clause still satisfied (xi-rope IS a mature Rust rope). Discharge: diff of that file.
- O-d7.3: J3-adjacent decision note: no new rope implementation is owned; lapce-xi-rope arrives as a dependency of the IDE fork, not a port. Discharge: cross-reference in the decision paragraph.

## Scope

Writes: this node file + the fleet manifest file. No crate changes (that is `d7m`'s scope).

## Decision (recorded 2026-08-10)

**D7, single text algebra — decision of record.** `lapce-xi-rope` (0.3.2) is the one text algebra for the IDE register: `RopeDelta` is the wire, computation, and rebase algebra end-to-end (matching the Lapce fork's own core). `rustyred-thg-text-model` re-backs on it (executed by d7m: Operation algebra deleted, fixtures green 16/16). The substrate owns its durable delta encoding — byte-offset insert/delete runs, base length, rev — converted at the store boundary, so history never couples to xi-rope's serde stability (executed: `encoding.rs` with round-trip + serde-stability tests). Leader-follower policy references STUDY-RHIZOME-REBASE. Fleet manifest amendment recorded (O-d7.2): the `fleet/andel` rope row's "mature Rust rope" clause is satisfied by lapce-xi-rope; the ropey mention is superseded with reason; no rope implementation is owned by the substrate (O-d7.3) — xi-rope arrives as a dependency of the IDE fork, not a port.

STATE: done (decision + fleet manifest amendment; migration executed by d7m).
