{
  "name": "@commonplace/root",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:rustyred": "node scripts/update-rustyred-source.mjs",
    "check": "node scripts/update-rustyred-source.mjs --check && cargo test --workspace",
    "check:rust": "cargo test --workspace"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  }
}
