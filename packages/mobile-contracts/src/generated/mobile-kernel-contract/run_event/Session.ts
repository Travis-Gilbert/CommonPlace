// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ContextSnapshotRef } from "../object_schema/ContextSnapshotRef";

/**
 * Long-lived continuity context (spec §6.1).
 *
 * Harness sessions are identified by `(agent_id, tenant)`; the product-level
 * `session_id` is minted by the control plane. Durable thread, message, and
 * session records live on the server and in RustyRed — the client cache is
 * never the system of record (spec §8.1 rule 7).
 */
export type Session = { sessionId: string, tenant: string, agentId?: string | null, title?: string | null, createdAt?: string | null, 
/**
 * Reference to the versioned context snapshot resolved when the session
 * began (spec §7.3): the run stores the reference, not only the expanded
 * prompt.
 */
contextSnapshot?: ContextSnapshotRef | null, };
