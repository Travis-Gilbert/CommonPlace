// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { JsonValue } from "../../serde_json/JsonValue";
import type { ViewKind } from "./ViewKind";
import type { ViewOrigin } from "./ViewOrigin";
import type { ViewTarget } from "./ViewTarget";

/**
 * List, table, board, calendar, timeline, map, graph, or scene presentation
 * of a type (spec §6.2). One record serves every surface — an agent-minted
 * view that appears on one surface and not the other is a defect
 * (spec §7.7 rule 3). `renderer` binds to the `commonplace`
 * `ViewDescriptor` renderer vocabulary.
 */
export type ViewDefinition = { viewId: string, name: string, viewKind: ViewKind, target: ViewTarget, renderer?: string | null, 
/**
 * Agents may create temporary views without changing data (spec §7.1
 * rule 4).
 */
temporary: boolean, origin: ViewOrigin, layout: { [key in string]: JsonValue }, };
