// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { Cardinality } from "./Cardinality";

/**
 * Wire projection of a register `FieldType::Relation` field
 * (spec §6.2): source type, target type, cardinality, inverse label, and
 * semantic meaning.
 */
export type RelationDefinition = { relationId: string, sourceObjectTypeId: string, targetObjectTypeId: string, cardinality: Cardinality, 
/**
 * Register field key this relation projects from (e.g. `{edge}_rel`).
 */
fieldKey?: string | null, inverseLabel?: string | null, semantic?: string | null, 
/**
 * Carried through from the register `FieldSpec` this relation projects
 * from. Without it a required relation is indistinguishable from an
 * optional one on the client, and the claimed 1:1 register binding would
 * silently drop enforcement.
 */
required: boolean, system: boolean, };
