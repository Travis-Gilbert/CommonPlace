// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { FieldDefinition } from "./FieldDefinition";
import type { ObjectType } from "./ObjectType";
import type { SchemaProposalStatus } from "./SchemaProposalStatus";
import type { ViewDefinition } from "./ViewDefinition";

/**
 * What an agent-produced schema proposal must show the user (spec §7.4):
 * the proposed change, why it is needed, similar existing fields or types,
 * existing records affected, migration behavior, reversibility, and a
 * preview of resulting views. This is what prevents synonymous type sprawl.
 */
export type SchemaProposal = { 
/**
 * Stable proposal hash minted by the register's proposal surface.
 */
proposalId: string, tenantId?: string | null, 
/**
 * The proposed object type (a register draft: empty id and anchor until
 * declared), when the proposal creates a type.
 */
proposedObjectType?: ObjectType | null, 
/**
 * The proposed field, when the proposal extends an existing type.
 */
proposedField?: FieldDefinition | null, targetObjectTypeId?: string | null, 
/**
 * Why the change is needed.
 */
rationale: string, 
/**
 * Similar existing fields or types the user should compare against.
 */
similarExisting: Array<string>, 
/**
 * Existing records affected.
 */
affectedRecordCount: number, 
/**
 * Migration behavior.
 */
migration: string, 
/**
 * Whether the change can be reversed.
 */
reversible: boolean, 
/**
 * Preview of resulting views.
 */
viewPreviews: Array<ViewDefinition>, status: SchemaProposalStatus, 
/**
 * Compare-and-set anchor for acceptance: promotion must name the anchor
 * it was reviewed against.
 */
expectedContentAnchor?: string | null, };
