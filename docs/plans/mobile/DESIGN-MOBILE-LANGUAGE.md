# DESIGN-MOBILE-LANGUAGE

Register: design language, pre-handoff. The visual and interaction language for the mobile app, complementing DESIGN-MOBILE-COMPANION (thesis and architecture) and its agency amendment. Four references contribute one organ each; the layer none of them have is what keeps this from reading as a clone. Companions: 13-AMENDMENT (speaker registers), 11-HANDOFF-CONSOLE-COLORATION (amended by section 2), SPEC-AGENCY-PROPOSAL-KERNEL (the Briefing), HANDOFF-MOBILE-EXCELLENCE (the execution layer this language governs).

License lanes, stated once: Plane is AGPL, patterns and composition only, no source, same lane as Twenty and OpenViking. Claude, Linear, and Capacities are closed products; observed behavior and measured values only. Nothing in this document is a source-code borrow.

## 1. One organ per reference

1. Claude contributes the material and the Composer. Content-forward, chrome-minimal, warm hue-constant ladder (section 2), and the large bottom-docked Composer: attach left, model or mode pill, mic, send; generous height; the input reads as an instrument, not a query bar. The Presence mark sits where their sparkle sits, and unlike theirs, ours is state-truthful.
2. Linear contributes the Inbox grammar for Triage: source icon, title, a reason line naming why the item is here and what happened last ("filed to X, needs you", "you approved, 2w"), quiet density, calm at every fill level. Swipes act only on reversible-class effects per the amendment; every swipe is a labeled signal.
3. Capacities contributes navigation by object type. A browse screen is a kind: type icon, view toggle (All, Overview), uniform rows opening object pages rendered from card templates. This is the card engine as an app; no bespoke browse screens exist.
4. Plane contributes the Home composition and the structural conventions: Your-work rows with live counts, Recent activity, sticky-like capture cards; structured empty states with exactly one action; the filter-chip row on every list screen.

The convergence adopted outright: Linear and Plane both ship the floating pill tab bar with a detached round primary action. Ours: pill carries Home, Chat, Triage, Search; the detached button is Capture, which gives capture the most reachable target in the app, which is what field organ means physically.

## 2. The Clay material (ratified; amends the coloration handoff)

The mechanism observed in Claude's dark mode is a hue-constant value ladder: every surface level is one warm hue at different lightness, no pure black below, no pure white above, borders one step from their surfaces. Same hue plus different lightness reads as one material under light; that is the depth-without-color effect.

The ruling: the coloration handoff's preset registry (named choice 6) gains **Clay**, a knob preset at tint hue near 55, chroma near 0.015, both modes, derived through the existing oklch engine and gated by the existing contrast machinery. On the console, Clay is a preset; IntelliJ Dark remains the console default because the console's identity is the pinned register. On mobile, **Clay is the default material**, because the phone is content-forward by thesis and owes JetBrains nothing. One engine, two defaults, zero new machinery.

## 3. The five surfaces in this language

1. Home: Plane's composition carrying Theorem's organs. The Briefing when one exists (proposal cards, hash-bound approve per the amendment), the relief ledger (what the harness caught and closed, quietly itemized; pronoia requires the catches be visible), Your-work rows with counts, Capture reachable without looking.
2. Chat: Claude's anatomy in Clay with Theorem's semantics. Thread center, Composer docked bottom at generous height, the sheen canvas as the one material moment (it will sing on OLED), the mark composing in the Composer, speaker registers on message content (Vollkorn yours, Plex Sans agent, machinery mono).
3. Triage: Linear's inbox grammar over the Needs-you queue. Reason lines from the filing engine's own vocabulary; accept, override with the Fix sheet, dismiss; card stack for focused runs; every decision a signal.
4. Browse: Capacities' object screens through the card engine. People, projects, topics, captures as kinds; object pages are card templates at full width; relation chips open their objects; mentions and the Action verb ride along because they are cards.
5. Capture: the share sheet target, quick note, camera; offline queue visible; each capture confirms its provenance in one line.

## 4. Type and number

The trinity carries at native scale with native ramps: Vollkorn for human authorship, IBM Plex Sans for agent voice and UI text, JetBrains Mono for machinery. `tabular-nums` on every count, timestamp, and spend figure. Dynamic Type respected; the ladder compresses gracefully rather than truncating meaning.

## 5. The layer no reference has

The Briefing as Home's morning form; the relief ledger; authorship rendered in ink and voice rather than avatars; honest refusal states naming their reason; `theorem://` links opening directly into objects; and approvals that are safe from a pocket because they are hash-bound. These are the product; the references are the floor it stands on.

## 6. Sourcing and the extraction follow-up

The RN component ledger starts empty and grows per surface; assistant-ui's React Native surface remains the named intent for Chat with its verify row standing. No RN component library is adopted by default; native primitives plus NativeWind over the token export are the base, and any adoption enters through the ledger with a license line.

Before the execution handoff dispatches, the extraction method runs on the three closed references the way it ran on Cursor and Twenty: screenshots from the owner's device, measured values (ladders, radii, spacing rhythms, type sizes, tab bar metrics), recorded in an APP-VALUES document per reference. This language document governs until exact values replace approximations; approximations never ship as pinned tokens.

## 7. Out of scope

The execution deliverables (HANDOFF-MOBILE-EXCELLENCE owns them, amended by 13-AMENDMENT's approval-grammar split), the Survey glance view, voice beyond the existing seams, and any console default change (Clay on the console is a preset, not a coup).
