# Console pivot decision log, 2026-07-17

Persisted to the repo because harness identity currently refuses at principal_resolution=unauthenticated. When encode is back, this document is the source for the memory flush. Every item below was decided in session with Travis and is in force.

1. The harness console lives in CommonPlace as `apps/console`. `Theorem/apps/harness-console` is superseded and should be tombstoned with a pointer here so two consoles never drift.
2. Greenfield strangler: `apps/web` keeps running untouched; the console grows surface by surface; porting from `apps/web` happens only by extraction into `packages/*` with zero CSS. The eslint fence and CI gate make direct imports impossible.
3. Composition doctrine: one material system (Int UI, pinned verbatim from JetBrains `expUI_dark.theme.json`), Twenty contributes record-surface structure only (TWENTY-APP-VALUES), Galley renders documents (`galley--bare`, planes bridged), bridged never blended.
4. The block-view contract carries via `packages/block-view`; `apps/web` copies became re-export shims; all renderers are rebuilt. SceneHost and its grammar renderers also carry, as `packages/scene-host`, with a re-token pass (they are engines, not chrome).
5. Governance carries: the library ledger rule, the motion register and interaction inventory, five states on every view, the register lint, no-theater (wired or named-unavailable). Amended: zero-animation-on-load is repealed; load entrances are designed and inventoried instead.
6. Terminology, permanent: the Search field (top toolbar), the Composer (the agent input), the Presence mark. The word omnibar is retired from specs and identifiers.
7. IA: the stripe splits into Surfaces (radio: Chat, Workspace, Index, Documents, Cards) and Companions (toggle: Files, Context, Thread). Dense horizontal artifacts are surfaces; sidebars hold navigation, context, and compact structure. Tables never mount in rails.
8. The Composer is the centerpiece: lower third, centered, wide not tall, material sheen as a state-bound canvas layer, the mark lives inside it. Banned by name: floating summon buttons and docked-right chat. Cmd/Ctrl L focuses it. 21st.dev components (subscription-licensed) adopt-and-edit: reuno-ui ai-input mechanics, binsalman surface material only. Corrected by Travis on 2026-07-18: supplied component code is the implementation starting point, never merely inspiration. For the binsalman port, preserve the layered translucent material as a subtle low-chroma wash, never added stripe geometry, above incidental demo controls or icon imports.
9. Scene OS in the shell: the wow morph is a transition between seeded surface states (snapshot and restore), not takeover machinery. The Do Not Downgrade gate re-points from porcelain to the Int UI register. Patent-drawing and layerstack visuals are content register, untouched by chrome rules.
10. Accent grammar roles carry across registers: pending decisions and actions on the accent slot, the learned register renders gold (`--ij-gold`). One accent per theme for chrome.
11. Theming: stock presets are pinned byte-stable; user theming is two knobs (background tint, highlight hue) via oklch re-anchoring, always behind the contrast gate with visible clamping. GitHub presets source from Primer primitives (MIT). Material Theme UI is not a source. Auto mode follows prefers-color-scheme.
12. Stripe icons, corrected by Travis on 2026-07-18: the current Noun-derived product glyphs are a provisional carryover from the prior Harness app, not the canonical Console direction. Add no new stripe glyphs from that family. A focused follow-up selects one coherent Console-specific icon language, updates the library ledger and icon gate together, and removes the provisional assets. Small control primitives remain register strokes until that pass.
13. Multibuffers (Zed concept): adopted in principle as a future read-first `excerpts.stack` descriptor over code_selection-shaped queries (search hits, mention passages, agent-touched-file review). In-place multi-file editing waits for the patch-proposal flow.
14. Files capability wiring, corrected by Travis on 2026-07-18: Project context and browsable Uploads are near-term product work, not decorative roots and not indefinitely deferred. Until their providers land, Files keeps all three capability roots visible with compact inline states (`Not connected`, live Memory count or connection state, `Empty`) and folds large real hierarchies by disclosure. An unavailable state is honest temporary degradation, never completion. The filing engine's per-project anchors remain the expected Project backend seed.
15. License lanes: Twenty is AGPL, values only, no component source; IntelliJ Community is Apache 2.0; mmdr and Primer are MIT; 21st.dev components under active subscription. OpenViking is AGPL, ideas and validation only, no code.
16. Deploy posture: alongside, not replace. The console ships at its own domain or subdomain and cuts over only when the strangler census (20-STRANGLER-CENSUS.md) shows a functional superset. Naming (commonplaces.ai) is parked as a launch-lane decision.
17. The theorem:// address scheme is minted (Theorem docs/plans/addressing/DESIGN-THEOREM-URI.md): `theorem://{tenant}/{kind}/{id}` with `#span` fragments and `?v=` graph-version pins; graph id is identity, projection path is display; objects expose as MCP resources carrying these addresses with tiered reads; addresses name, sessions grant. Emitters v1: mention chips, cards, Survey clippings, Composer and Search field acceptance, Tauri deep-link on desktop, Expo linking on mobile.
18. Mobile is the field organ (docs/plans/mobile/DESIGN-MOBILE-COMPANION.md): capture, chat, triage, reader, glanceable home; one contract two renderers over packages/block-view; never the IDE in a small window. Triage swipes are labeled signals for the training pipeline.
19. Canonical host, corrected by Travis on 2026-07-18: `https://v2.theoremharness.com` is the primary CommonPlace frontend and is served by `apps/console`. `https://app.theoremharness.com` and `apps/web` are legacy surfaces. New product login and account work lands in the Console descriptor system.
20. Account tenancy: GitHub verifies the user, the case-preserving verified login becomes that user's tenant, and missing identity is refused. `Travis-Gilbert` is the owner's authorized tenant, never a product fallback.
21. Login rollout: before GitHub OAuth credentials are configured, the Console may admit only the explicit non-default `CONSOLE_HARNESS_TENANT` service identity so the existing owner workspace remains usable. Once both GitHub credentials exist, that compatibility path switches off automatically and every browser request requires its verified session.
22. Composer destinations are real backend contracts: `Theorem` sends the hosted ACP turn unchanged; `Web search` sends the validated `web` capability only after the authenticated CommonPlace backend advertises it, and the Console route rechecks that advertisement before dispatch.
23. Islands and material (Spec 34, amendment 35, 2026-07-20): shell paint leaves CSS for a full-viewport WebGL Material Layer (SDF islands, gutter shadows, edge falloff, grain). Color lives in the ground (terracotta pools); islands stay near-neutral; toolbar and status are frame-resident; ~10px gutters and ~r10 islands; Manrope 600 island headers; overlay scrollbars; primary nav rows 36–40px; dark-first. Proof sketch: `docs/plans/console/islands-material/islands-material-v2.html`. Harness plan: `plan:cab62c608af28b18`. Amendment: `35-AMENDMENT-MATERIAL-REVIEW.md`.

Status notes as of this writing: rounds 1 and 2 merged (PR 57, 58), handoffs DIAGRAM-REGISTERS-AND-MODEL and CARDS-ACTIONS-MENTIONS dispatched (PR 60 open), IA dispatched, COLORATION committed and ready, console deployed alongside at v2.theoremharness.com, harness identity still refused (request id req_011Cd88jV49or33hTNDqoP1V).
