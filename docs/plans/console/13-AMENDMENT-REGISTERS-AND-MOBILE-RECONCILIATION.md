# AMENDMENT-REGISTERS-AND-MOBILE-RECONCILIATION

Register: amendment. Reconciles four received documents (NORTH-STAR-LIFE-LOOP, HANDOFF-VERIFIED-COGNITION, HANDOFF-MOBILE-EXCELLENCE, SPEC-SPEAKER-REGISTERS) with the console-era corpus (00-DECISIONS, DESIGN-MOBILE-COMPANION, SPEC-AGENCY-PROPOSAL-KERNEL, the Int UI register). Amends in place of edits; the named documents stand as written except where a ruling below supersedes a value.

## 1. Corpus placement (which document governs what)

1. NORTH-STAR-LIFE-LOOP is the governing north star of the agency lane: senses, address, standing, the six-segment loop. Everything below it is a segment implementation.
2. SPEC-AGENCY-PROPOSAL-KERNEL implements Act and half of Judge (proposals, exact approval, grants, trust dimensions). The life loop's "per-step approval graduating to earned autonomy through grants" is the kernel; no daylight between them.
3. HANDOFF-VERIFIED-COGNITION implements the other half of Judge as a user-facing lane (dual-entry verification). Its named choice 1 (direct surface plus agent affordance over one executor) is the same dual-door doctrine as the standing decisions; the corpus is consistent.
4. DESIGN-MOBILE-COMPANION is the mobile design layer; HANDOFF-MOBILE-EXCELLENCE is its execution layer. The brief's thesis (field organ) and the excellence handoff's thesis (supervision) compose: supervision is what the field organ does to running work. Approvals are the hero surface on both.
5. SPEC-SPEAKER-REGISTERS governs authorship encoding everywhere conversational content renders. Its values were written against porcelain and re-resolve under section 2; its roles carry unchanged.
6. The life loop's "the Index is the inbox" confirms HANDOFF-INDEX's thesis and extends its source list (screened calls, shielded email, intercepted reminders, dread-queue items); the Index handoff needs no change, the connectors simply gain future kinds. Its shield rule (the gate ranks, never silently drops) is already the filing engine's abstention posture.

## 2. The speaker-register ruling under Int UI

The conflict: SPEC-SPEAKER-REGISTERS assigns oxblood to the human, teal to the agent, gold to memory, on parchment with Vollkorn, Plex Sans, and JetBrains Mono. The console decisions assign the Int UI accent (blue) to pending decisions and actions, gold to learned, on dark chrome. The Presence mark has three color histories (gold base with oxblood flash in the sourcing addendum; accent-slot flash in the console handoffs; teal base with teal contraction in speaker-registers D3).

The ruling:

1. Two grammars coexist by scope, and the scopes do not overlap. The chrome grammar (Int UI accent for interactive and pending, gold for learned) governs chrome: toolbars, stripes, tabs, selections, run states, tool windows. The speaker grammar (human ink, agent voice, memory gold, reserved destructive) governs conversational content: chat messages, margin threads, annotations, approval cards, proposal cards. A message bubble is content; the tab strip above it is chrome. The lint enforces the boundary by surface class.
2. The roles carry verbatim; the values re-resolve for the register. Human ink stays the oxblood family, re-tuned for AA on the dark chrome and on the light editor well (two values, one token). Agent voice is teal, seeded from the March terminal family and tuned the same way, chosen to remain distinguishable from the Int UI accent blue at a glance (the accent is the system speaking as machinery; teal is the agent speaking as a voice; blue and teal must not blur, which the tuning pass verifies numerically in hue distance). Memory gold is already `--ij-gold` and does not change. Destructive red remains reserved and rare.
3. Typography trinity adopted whole, and it is nearly free: Vollkorn (human authorship), IBM Plex Sans (agent voice), JetBrains Mono (machinery regardless of speaker) are exactly markdown-theory's loaded faces. Galley already carries them; the chat surface assigns them by speaker. Chrome remains Inter; the trinity governs content surfaces only. Grayscale legibility of speaker distinction by face alone is the acceptance test, carried from the spec.
4. The Presence mark ruling: base teal per speaker-registers D3, since the mark is the agent's identity and identity belongs to the voice register. The commit moment is the teal contraction with brightness lift. This supersedes the sourcing addendum's gold base and oxblood flash, and supersedes the console handoffs' accent-slot flash language. The interrupted state's brief oxblood edge on user interruption stands (the human's hand arriving is human ink, correctly). Gold never marks the mark; gold marks memory.
5. Token layer: the spec's `--cp-*` register names land as the cross-platform names in `packages` token exports (web, mobile tokens.ts, Tailwind mapping per its D1), bridged into the console register files; the register lint gains the two spec rules (raw values fail; human ink on a destructive variant fails).

## 3. Mobile reconciliation

1. HANDOFF-MOBILE-EXCELLENCE is adopted as the mobile execution handoff with these console-era deltas, and is otherwise dispatchable as written:
   - Tokens: the theme layer derives from the Int UI ladders plus the speaker registers of section 2, superseding porcelain-era values. Contrast receipts stay, extended to the new tokens per speaker-registers D6.
   - The approval grammar split: D1's four decisions (allow once, allow for session, always for this pattern, deny) bind to the kernel as follows. Allow once and allow for session are exact approvals (hash-bound, expiring). Always for this pattern is a grant proposal and routes to the full-screen deliberate signing flow from DESIGN-MOBILE-COMPANION section 5.6; it is never a card tap or a swipe. Deny records the dismissal signal. The minimum-visible-time guard is adopted as kernel-grade (an approval control that can be tapped before it is seen violates exact approval in spirit) and belongs beside the kernel's gates when the mobile slice lands.
   - Swipe approvals in the inbox apply only to reversible-class effects; visible-artifact and irreversible classes require the opened card. The effect contract, not the surface, decides which affordance is available.
   - The WeaveSpinner stays per speaker-registers D5: unknown-duration machine work is the harness laboring, not a speaker; it is explicitly untouched. The mark and the spinner coexist with distinct jobs, as the sourcing addendum always held.
   - Voice: the existing server seams (STT off by default, TTS ElevenLabs default Kokoro-swappable, keys never in the bundle) upgrade the mobile brief's voice posture from deferred to seam-exists; D4's degrade-cleanly rule (mic hides when the seam is off) is the no-theater rule applied to voice.
2. DESIGN-MOBILE-COMPANION deltas (amended here rather than edited):
   - Section 4 gains the relief ledger on Home: the life loop's Prove segment. What the harness caught, closed, and saved renders as a quiet ledger, because pronoia needs evidence rendering; the loop is only felt when the catches are visible.
   - Section 5.2's reserved slot fills: verified cognition reaches mobile as direct lenses (Decision, Consistency) reachable without addressing the agent, dual-entry preserved at phone scale; the verified state on proposal cards renders solver and verifier receipts through the shared why-trace component. VC4's ear rides the voice seams above.
   - The current-condition correction: the app is not bare. HANDOFF-MOBILE-EXCELLENCE records expo-router tabs, thread and room and object routes, a capture queue, notifications, theme tokens with contrast receipts, and the WeaveSpinner as existing (PR 25 lineage). The brief's inventory-before-building verify row is therefore mandatory, and the strangler census's PR 25 row needs a re-read (it was classified porcelain-only from its title; the excellence handoff claims mobile contents in the same PR). Census errata filed in section 5.
   - "Chat is not its job" (life loop rung 5) reconciles as job hierarchy, not exclusion: approvals are the hero, chat is present. The excellence handoff's thesis sentence governs.
3. The composer terminology sweep applies to the excellence handoff on dispatch: its "omnibar" references read as the mobile Composer and Search per decision 6.

## 4. Verified cognition placement

VC1's Decision surface is a lens in the console registry and a mobile lens; both are the direct door. The kernel consumes VC receipts as `verifier_receipt_refs` on proposals, which is the two lanes meeting: verification grades what the watcher noticed, the proposal carries the receipt, the approval card renders it through the why-trace. No new machinery is needed for the meeting; the reference systems already join at the receipt.

## 5. Census errata and dispatch order

1. Census: PR 25's row upgrades from lane C (porcelain-only) to a split row: porcelain physics (C) plus the mobile app foundation (F, carried). The deep pass verifies the split against the PR contents.
2. Dispatch order for this lane: speaker-register token layer and lint (section 2.5) first, because chat surfaces land in HANDOFF-CONSOLE-IA and should land in the right registers rather than be repainted; then the kernel (V1 tenant refusal first, per its own gates); then HANDOFF-MOBILE-EXCELLENCE with the section 3.1 deltas; verified cognition's VC1 when the solver arms verify.
3. The life loop's ladder is the lane's order of work beyond that, as written, order not calendar.
