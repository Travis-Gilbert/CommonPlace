target/
node_modules/
.DS_Store
dist/
coverage/
*.tsbuildinfo
apps/web/.next/
apps/web/out/
apps/web/.harness/reports/
apps/commonplace-api/target/
apps/desktop/src-tauri/target/

# Secrets: real provider keys live in .env, never committed. .env.example is the
# tracked template (placeholders only).
.env
.env.*
!.env.example

# Vendored chat register build output
apps/chat/dist/

# The VS Code pack's bundles, and the fork pipeline's upstream checkout. The
# checkout is a clone of microsoft/vscode plus the patch queue; it is rebuilt by
# scripts/build.sh and must never be committed.
apps/theorem-vscode/dist/
packaging/commonplace-studio/build/

# Local pnpm content-addressable store (agent / CI fallback when user store missing)
.pnpm-store/
